{% extends 'base.html.twig' %}

{% block title %}Gestion de l'organigramme |
	{{ parent() }}
{% endblock %}

{% block body %}

	<div class="bordure">
		{% for user in users %}

			<div class="card col-3 m-1">
				<div class="carte-entete membresBureau">
					<p class="role_user">{{ user.referents }}</p>
				</div>
				<div class="card-body  d-flex flex-column">
					<div class="d-flex flex-row justify-content-center">
						<p class="card-tex nomPrenom">{{ user.prenom }}</p>
						<p class="card-tex nomPrenom">&nbsp{{ user.nom }}</p>
					</div>
					<!--<div class="d-flex flex-row justify-content-center">
					<a href="{{ path('use_edit', {'id': user.id}) }}" class="button-modifier-profil-admin btn btn-success">Modifier</a>
					</div>-->
				</div>
			</div>
		{% endfor %}
	</div>

{% endblock %}
